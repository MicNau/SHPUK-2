# Спецификация формата JSON-дескриптора дома

**Версия формата**: 2.0
**Статус**: согласовано
**Файлы дескрипторов**: `assets/houses/house_type_*.json`

**Канонические примеры** (демонстрируются в тестовом приложении `test-house.html`):

| Файл | Дом | Перимeтр | Крыша |
|------|-----|----------|-------|
| `house_type_01.json` | Одноэтажный с вальмовой | прямоугольник | `hip` |
| `house_type_02.json` | Одноэтажный с двускатной | прямоугольник | `gable` |
| `house_type_03.json` | Г-образный одноэтажный   | L-форма (turn=−90 в выступе) | `flat` |

**Статус JS-реализации**:
- 🟡 `loadHouseType` / `buildHouseFromDescriptor` / `transformParametricModule` / `applyMaterialOverride` реализованы в **изолированном тестовом приложении** `test-house.js` — для проверки парадигмы и итеративной отладки. См. `ARCHITECTURE.md` → «Тестовое приложение модульной сборки» и `HOUSE_MODULES_SPEC.md` → «5.2.1. Уточнения алгоритма».
- ❌ В основной фронтенд (`viewer3d-core.js`) пока не интегрировано — текущий 3D-дом там строится процедурно (`buildHouseMeshes`) как fallback.

### Изменения v2.0

- Секция `modules`: размеры окон и дверей заданы как диапазоны (`min`/`max`/`default`), а не фиксированные значения
- Добавлены параметрические двери: распашные (1/1.5/2 створки) + раздвижные (1/2 створки)
- Угловые элементы заменены на столб (`pillar`): `pillar_size` в `constraints`
- Добавлена секция `roof_windows` для мансардных (velux) и слуховых (dormer) окон
- Добавлен `modules_override` на уровне этажа для переопределения размеров окон/дверей

---

## 1. Обзор

Каждый тип дома описывается одним JSON-файлом. Файл определяет:

- Метаданные (название, описание)
- Ограничения параметров (допустимые диапазоны площади, высоты и т.д.)
- Тип крыши
- Каталог модулей с диапазонами размеров
- Этажи (один или несколько), каждый со своей планировкой
- Планировку каждого этажа — как **обход периметра по часовой стрелке**
- Мансардные / слуховые окна на крыше
- Дополнительные элементы (дымоход, водостоки, карниз)
- Карту заменяемых материалов

---

## 2. Корневая структура

```jsonc
{
  // ── Метаданные ──────────────────────────────────────
  "id":          "type_01",                             // string, уникальный ID
  "name":        "Одноэтажный дом",                     // string, отображается в UI
  "description": "С вальмовой крышей",                  // string, подпись

  // ── Общие ограничения (для всего дома) ──────────────
  "constraints": {
    "base_h":          { "min": 50, "max": 120, "step": 10, "default": 80, "unit": "cm" },
    "roof_angle":      { "min": 15, "max": 45,  "step": 5,  "default": 25, "unit": "deg" },
    "wall_thickness":  0.20,   // метры, фиксировано
    "pillar_size":     0.20    // метры, сечение углового столба (по умолчанию = wall_thickness)
  },

  // ── Тип крыши ───────────────────────────────────────
  "roof_type": "hip",         // "hip" | "gable" | "gable_cross" | "flat"

  // ── Каталог модулей (параметры проёмов) ─────────────
  "modules": { /* ... см. раздел 2.1 ... */ },

  // ── Этажи ───────────────────────────────────────────
  "floors": [ /* ... см. раздел 3 ... */ ],

  // ── Мансардные / слуховые окна ──────────────────────
  "roof_windows": [ /* ... см. раздел 7 ... */ ],

  // ── Доп. элементы ───────────────────────────────────
  "features": { /* ... см. раздел 8 ... */ },

  // ── Карта материалов ────────────────────────────────
  "materials_map": { /* ... см. раздел 9 ... */ }
}
```

### Обязательные поля верхнего уровня

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор типа дома |
| `name` | string | Название для отображения в UI |
| `constraints` | object | Общие ограничения (фундамент, крыша, толщина стены, сечение столба) |
| `roof_type` | string | Тип крыши: `"hip"` (вальмовая на каждом rect декомпозиции), `"gable"` (двускатная на главном rect + вальмовые на пристройках), `"gable_cross"` (двускатная на каждом rect — cross-gable), `"mansard"` (мансарда: gable + опциональный knee wall, см. раздел 8.1), `"flat"` (плоская с парапетом) |
| `modules` | object | Каталог модулей с диапазонами размеров (см. раздел 2.1) |
| `floors` | array | Массив этажей (минимум 1) |
| `materials_map` | object | Перечень заменяемых материалов |

### Необязательные поля

| Поле | Тип | Описание |
|------|-----|----------|
| `description` | string | Подпись в UI |
| `roof_windows` | array | Мансардные / слуховые окна (см. раздел 7) |
| `features` | object | Дымоход, водостоки, карниз |

### 2.1. Каталог модулей (`modules`)

Секция `modules` задаёт параметры всех оконных и дверных модулей, используемых
в `perimeter.facade`. Размеры заданы как **диапазоны** с дефолтными значениями.
Код использует эти значения для расчёта `fill`-заполнителей, позиционирования проёмов
и трансформации дочерних объектов GLB.

```jsonc
"modules": {
  // ── Окна (параметрические) ──────────────────────────
  "window_single": {
    "w": { "min": 0.60, "max": 1.40, "default": 0.90 },
    "h": { "min": 0.60, "max": 1.80, "default": 1.20 },
    "y": { "min": 0.30, "max": 1.50, "default": 0.90 },
    "frame_profile": 0.05,
    "sill_overhang": 0.03,
    "glb": "mod_window_single.glb"
  },
  "window_double": {
    "w": { "min": 1.00, "max": 2.00, "default": 1.40 },
    "h": { "min": 0.60, "max": 1.80, "default": 1.20 },
    "y": { "min": 0.30, "max": 1.50, "default": 0.90 },
    "frame_profile": 0.05,
    "sill_overhang": 0.03,
    "mullions": ["v"],
    "glb": "mod_window_double.glb"
  },
  "window_wide": {
    "w": { "min": 1.80, "max": 3.00, "default": 2.40 },
    "h": { "min": 0.80, "max": 2.00, "default": 1.50 },
    "y": { "min": 0.20, "max": 1.20, "default": 0.60 },
    "frame_profile": 0.05,
    "sill_overhang": 0.03,
    "mullions": ["v", "h"],
    "glb": "mod_window_wide.glb"
  },

  // ── Мансардные окна (параметрические) ───────────────
  "window_velux": {
    "w": { "min": 0.55, "max": 1.40, "default": 0.78 },
    "h": { "min": 0.60, "max": 1.60, "default": 0.98 },
    "frame_profile": 0.04,
    "glb": "mod_window_velux.glb"
  },
  "dormer": {
    "w":     { "min": 1.00, "max": 2.50, "default": 1.50 },
    "h":     { "min": 1.00, "max": 2.00, "default": 1.40 },
    "depth": { "min": 0.60, "max": 1.50, "default": 1.00 },
    "frame_profile": 0.05,
    "sill_overhang": 0.03,
    "glb": "mod_dormer.glb"
  },

  // ── Двери распашные (параметрические) ───────────────
  "door_single": {
    "w": { "min": 0.80, "max": 1.00, "default": 0.90 },
    "h": { "min": 2.00, "max": 2.40, "default": 2.10 },
    "y": 0.00,
    "leaves": 1,
    "mechanism": "swing",
    "frame_profile": 0.05,
    "glb": "mod_door_single.glb"
  },
  "door_onehalf": {
    "w": { "min": 1.10, "max": 1.40, "default": 1.20 },
    "h": { "min": 2.00, "max": 2.40, "default": 2.10 },
    "y": 0.00,
    "leaves": 1.5,
    "mechanism": "swing",
    "frame_profile": 0.05,
    "glb": "mod_door_onehalf.glb"
  },
  "door_double": {
    "w": { "min": 1.20, "max": 1.80, "default": 1.40 },
    "h": { "min": 2.00, "max": 2.40, "default": 2.10 },
    "y": 0.00,
    "leaves": 2,
    "mechanism": "swing",
    "frame_profile": 0.05,
    "glb": "mod_door_double.glb"
  },

  // ── Двери раздвижные (параметрические) ──────────────
  "door_slide_single": {
    "w": { "min": 0.80, "max": 1.20, "default": 1.00 },
    "h": { "min": 2.00, "max": 2.40, "default": 2.10 },
    "y": 0.00,
    "leaves": 1,
    "mechanism": "slide",
    "frame_profile": 0.05,
    "glb": "mod_door_slide_single.glb"
  },
  "door_slide_double": {
    "w": { "min": 1.40, "max": 3.00, "default": 2.00 },
    "h": { "min": 2.00, "max": 2.40, "default": 2.30 },
    "y": 0.00,
    "leaves": 2,
    "mechanism": "slide",
    "frame_profile": 0.05,
    "glb": "mod_door_slide_double.glb"
  }
}
```

#### Поля модуля окна

| Поле | Тип | Описание |
|------|-----|----------|
| `w` | range | Ширина проёма (включая раму), метры |
| `h` | range | Высота проёма (включая раму), метры |
| `y` | range | Расстояние от уровня пола этажа до нижнего края проёма, метры |
| `frame_profile` | number | Ширина профиля рамы, метры (фиксировано) |
| `sill_overhang` | number | Свес подоконника за раму с каждой стороны, метры |
| `mullions` | array | Список импостов: `"v"` — вертикальный, `"h"` — горизонтальный |
| `glb` | string | Имя GLB-файла в `assets/modules/windows/` |

#### Поля модуля двери

| Поле | Тип | Описание |
|------|-----|----------|
| `w` | range | Ширина проёма (включая раму), метры |
| `h` | range | Высота проёма (включая раму), метры |
| `y` | number | Всегда `0.00` — двери начинаются от пола |
| `leaves` | number | Количество створок: `1`, `1.5`, `2` |
| `mechanism` | string | `"swing"` (распашная) или `"slide"` (раздвижная) |
| `frame_profile` | number | Ширина профиля коробки, метры |
| `glb` | string | Имя GLB-файла в `assets/modules/doors/` |

#### Формат range

```jsonc
{ "min": 0.60, "max": 1.40, "default": 0.90 }
```

- `default` — используется, если в элементе фасада размер не переопределён
- `min` / `max` — для валидации переопределённых значений

#### Ключи модулей и ссылки из фасада

**Ключ** объекта (`window_single`, `door_slide_double` и т.д.) — это ID модуля,
на который ссылаются элементы фасада: `{ "window": "single" }` → ищет `window_single`,
`{ "door": "slide_double" }` → ищет `door_slide_double`.

**Правила формирования ключа**: `<тип>_<вариант>`, где тип = `window` | `door`.
В элементе фасада пишется только вариант: `{ "window": "single" }`, `{ "door": "single" }`.

Новые типы окон/дверей добавляются расширением этой секции:
```jsonc
"window_arch":   { "w": { "min": 0.80, "max": 1.20, "default": 1.00 }, "h": ..., "glb": "mod_window_arch.glb" },
"door_garage":   { "w": { "min": 2.50, "max": 4.00, "default": 3.00 }, "h": ..., "glb": "mod_door_garage.glb" }
```

---

## 3. Этаж (`floors[i]`)

Каждый элемент массива `floors` описывает один этаж:

```jsonc
{
  "id":       "floor_1",           // string, уникальный ID этажа
  "label":    "Первый этаж",       // string, отображается в UI

  "constraints": {
    "area":    { "min": 40, "max": 100, "step": 5,  "default": 80,  "unit": "m2" },
    "floor_h": { "min": 270, "max": 360, "step": 10, "default": 300, "unit": "cm" }
  },

  "vars": {
    "L": "sqrt(area * 1.6)",
    "W": "sqrt(area / 1.6)"
  },

  // Переопределение размеров окон/дверей для этого этажа (опционально)
  "modules_override": {
    "window_single": { "h": 1.40, "y": 0.80 }
  },

  "perimeter": [ /* ... см. раздел 4 ... */ ]
}
```

### Поля этажа

| Поле | Тип | Обязательное | Описание |
|------|-----|:---:|----------|
| `id` | string | ✓ | Уникальный ID, используется в `S.wallZones` |
| `label` | string | ✓ | Название для UI |
| `constraints` | object | ✓ | Допустимые значения площади и высоты этажа |
| `vars` | object | ✓ | Параметрические переменные (см. раздел 5) |
| `perimeter` | array | ✓ | Обход периметра (см. раздел 4) |
| `modules_override` | object | — | Переопределение размеров окон/дверей для этажа |
| `area_factor` | number | — | Множитель площади относительно `params.area` (1.0 по умолчанию). Для 1.5-этажа: floor_2.area_factor = 0.5 даст вдвое меньшую площадь второго этажа. |
| `start_offset` | object | — | `{ x: <expr>, z: <expr> }` — смещение первой точки периметра от (0,0). Выражения вычисляются от `vars` этажа. Используется для центрирования верхних этажей внутри нижнего (например, мансарды). По умолчанию (0,0). |

### Ограничения этажа (`constraints`)

| Ключ | Что задаёт | Единицы |
|------|-----------|---------|
| `area` | Площадь этажа в плане | м² |
| `floor_h` | Высота этажа (от пола до потолка) | см |

Каждое ограничение — объект:
```jsonc
{ "min": 40, "max": 100, "step": 5, "default": 80, "unit": "m2" }
```

Код конфигуратора:
- Устанавливает `min`, `max`, `step` на элементы ввода
- Округляет значения до шага: `value = round(raw / step) * step`
- Не допускает выход за диапазон

### Переопределение размеров модулей (`modules_override`)

Позволяет задать **другие дефолтные размеры** окон и дверей для конкретного этажа.
Переопределённые значения мержатся с корневыми `modules`:

```jsonc
// Корневой modules:
"window_single": { "w": { ... "default": 0.90 }, "h": { ... "default": 1.20 }, "y": { ... "default": 0.90 } }

// modules_override на 1-м этаже:
"window_single": { "h": 1.40, "y": 0.80 }

// Результат для 1-го этажа:
// w = 0.90 (из корневого), h = 1.40 (переопределён), y = 0.80 (переопределён)
```

Значения в `modules_override` — **числа** (не range-объекты), т.к. это конкретные
дефолты для этажа. Элемент фасада всё ещё может переопределить размер дополнительно.

**Приоритет**: элемент фасада > `modules_override` этажа > корневой `modules.default`

### Многоэтажность

- Этажи собираются **снизу вверх** (первый элемент `floors` = нижний этаж)
- Между этажами автоматически добавляется **перекрытие** толщиной 0.2 м
- Каждый этаж может иметь **свою площадь и пропорции**
- Если верхний этаж меньше нижнего — образуется уступ (пользователь может вручную разметить на нём балкон)
- Верхний этаж **автоматически центрируется** относительно нижнего

---

## 4. Обход периметра (`perimeter`)

Планировка задаётся как **обход контура по часовой стрелке** (turtle graphics).

### 4.1. Начальные условия

- Стартовая точка: `[0, 0]` (верхний-левый угол на плане)
- Начальное направление: **вправо** (+X)
- Система координат: X — вправо, Z — вниз (вид сверху)

### 4.2. Типы команд

Массив `perimeter` содержит чередующиеся команды двух типов:

#### Команда `run` — движение и рисование стены

```jsonc
{
  "run": "L",              // string — имя переменной из vars (длина участка)
  "facade": [ /* ... */ ]  // array | string — содержимое стены (см. раздел 6)
}
```

#### Команда `turn` — поворот + установка столба

```jsonc
{ "turn": 90 }   // число — угол поворота в градусах
```

| Значение | Направление | Эффект на контуре |
|----------|-------------|-------------------|
| `90` | По часовой стрелке | Обычный угол (внутренний угол дома) |
| `-90` | Против часовой стрелки | Создаёт **выступ** (внешний угол дома) |
| `180` | Разворот | Очень редко, для U-образных элементов |

**Каждый `turn`** автоматически ставит **столб (pillar)** в текущей точке.
Столб — кубоид квадратного сечения (`pillar_size × pillar_size`), универсален
для любого типа угла (внешний, внутренний — без разницы).

### 4.3. Столбы и длина стен

```
Вид сверху:

    стена A    pillar    стена B
  ──────────┐  ┌──┐  ┌──────────
            │  │██│  │
            └──┘  └──┘
```

- На каждый `turn` ставится один `pillar`
- Длина run — это расстояние **от центра столба до центра столба**
- Эффективная длина стены = `run_length - pillar_size`
- Это учитывается автоматически в расчёте `fill`

### 4.4. Правило замкнутости

Контур **обязан быть замкнутым**: после выполнения всех команд текущая точка должна
совпасть с начальной `[0, 0]`. Код проверяет это и выдаёт предупреждение при ошибке.

### 4.5. Порядок команд

Команды `run` и `turn` обычно чередуются:
```
run → turn → run → turn → run → turn → run → turn   (прямоугольник: 4 run + 4 turn)
```

Для сложных планов (L, T, крест) количество команд больше, но принцип тот же.

### 4.6. Комментарии

JSON не поддерживает комментарии, но можно использовать `"_comment"`:
```jsonc
{ "_comment": "── Северная стена →" }
```
Код игнорирует элементы без `run` и без `turn` — комментарий-только-объект пропускается.

**⚠ Важно: НЕ помещайте `_comment` в один объект с `turn`/`run`** — поле `_comment` должно быть **в отдельном элементе массива**:

```jsonc
// ✅ ПРАВИЛЬНО:
{ "_comment": "── ВНЕШНИЙ угол ──" },
{ "turn": -90 },

// ❌ НЕПРАВИЛЬНО (turn может молча потеряться в парсере):
{ "turn": -90, "_comment": "── ВНЕШНИЙ угол ──" }
```

(Реализация JS-парсера должна корректно обрабатывать оба варианта, но безопаснее придерживаться отдельных элементов — этот же стиль используется в канонических примерах `house_type_*.json`.)

---

## 5. Параметрические переменные (`vars`)

### 5.1. Формат

```jsonc
"vars": {
  "L":    "sqrt(area * 1.6)",      // длина дома
  "W":    "sqrt(area / 1.6)",      // ширина дома
  "wing": "W * 0.3",              // ширина крыла
  "arm":  "L * 0.25"              // длина крыла
}
```

Каждая переменная — это строка-выражение, которое вычисляется в числовое значение (метры).

### 5.2. Доступные в выражениях величины

| Имя | Откуда | Описание |
|-----|--------|----------|
| `area` | Из UI (constraints этажа) | Площадь этажа, м² |
| Другие переменные из `vars` | Этот же блок | Можно ссылаться на уже определённые |

### 5.3. Поддерживаемые операции

| Операция | Пример | Описание |
|----------|--------|----------|
| `+` `-` `*` `/` | `"area * 1.6"` | Арифметика |
| `sqrt()` | `"sqrt(area * 1.6)"` | Квадратный корень |
| `()` | `"(L + W) / 2"` | Группировка |
| Числовые литералы | `"1.6"`, `"0.3"` | Константы |

### 5.4. Порядок вычисления

Переменные вычисляются **сверху вниз** — каждая может ссылаться только на уже
определённые выше переменные и на `area`.

```jsonc
"vars": {
  "L":    "sqrt(area * 1.6)",    // OK: использует area
  "W":    "sqrt(area / 1.6)",    // OK: использует area
  "wing": "W * 0.3",            // OK: W уже определена
  "diag": "sqrt(L*L + W*W)"     // OK: L и W уже определены
}
```

### 5.5. Когда пересчитываются

Переменные пересчитываются при каждом изменении параметров пользователем
(ползунок площади, поле высоты и т.д.). После пересчёта контур перестраивается.

---

## 6. Фасад (`facade`)

Описывает содержимое стены на одном `run` — что расположено на этом участке
**слева направо по ходу движения**.

### 6.1. Формат — массив элементов

```jsonc
"facade": [
  { "wall": 1.2 },            // глухая стена 1.2 м
  { "window": "single" },     // одностворчатое окно (размеры из modules)
  { "wall": "fill" },         // заполнитель
  { "window": "single" },     // ещё окно
  { "wall": 1.2 }             // глухая стена 1.2 м
]
```

### 6.2. Формат — автоматические окна

```jsonc
"facade": "auto_windows",     // строка вместо массива
"window_type": "single",      // какой тип окна расставлять
"min_margin": 1.0             // мин. отступ от краёв и между окнами (по умолчанию 1.0)
```

Код рассчитает:
```
effectiveLen = runLength - pillar_size
winW = modules[window_type].w.default
winCount = floor(effectiveLen / (winW + min_margin * 2))
margin = (effectiveLen - winCount * winW) / (winCount + 1)
```

### 6.3. Типы элементов фасада

#### Глухая стена

```jsonc
{ "wall": 2.0 }       // фиксированная длина, метры
{ "wall": "fill" }     // заполнитель (вычисляется)
```

**`"fill"`** — оставшееся после размещения фиксированных элементов место делится
**поровну** между всеми `"fill"` в этом `run`.

Алгоритм:
```
effectiveRun = вычисленная длина run - pillar_size  // стена между столбами
fixedLen     = Σ(фиксированных wall) + Σ(ширин окон/дверей из модулей)
fillCount    = количество элементов с "fill"
eachFill     = (effectiveRun - fixedLen) / fillCount
```

Если `eachFill < 0.1` → ошибка: элементы не помещаются в стену.

**⚠ Если в фасаде НЕТ ни одного `{"wall": "fill"}`** (`fillCount === 0`), то `fixedLen` ДОЛЖНА точно совпадать с `effectiveRun`. Иначе:
- `fixedLen < effectiveRun` → видимая **дыра** в фасаде на `effectiveRun − fixedLen` метров (стена не построена в этом интервале)
- `fixedLen > effectiveRun` → элементы выходят за пределы стены и накладываются на соседние

Парсер `test-house.js` логирует warn в этом случае (`[fills] ⚠ no fills, but gap N м …`). Рекомендация: **всегда добавляйте хотя бы один `{"wall": "fill"}`** в фасад с переменными ширинами стен — это автоматически абсорбирует разницу при изменениях `area` через UI.

#### Окно

```jsonc
{ "window": "single" }                                 // дефолтные размеры из modules
{ "window": "double" }                                 // двустворчатое
{ "window": "wide" }                                   // панорамное
{ "window": "single", "w": 1.10 }                     // переопределена ширина
{ "window": "single", "h": 1.40 }                     // переопределена высота
{ "window": "single", "y": 1.2 }                      // переопределена высота подоконника
{ "window": "single", "w": 1.10, "h": 1.40, "y": 0.80 }  // всё переопределено
{ "window": "single", "w": 0.60, "h": 0.50, "y": 1.50 }  // маленькое окно (санузел)
```

**Приоритет размеров**: элемент фасада > `modules_override` этажа > `modules.*.default`

Размеры и параметры из секции `modules`:

| Поле | Описание |
|------|----------|
| `w` | Ширина проёма (включая раму), используется при расчёте `fill` |
| `h` | Высота проёма (включая раму) |
| `y` | Расстояние от уровня пола этажа до нижнего края проёма |
| `frame_profile` | Ширина профиля рамы (для трансформации GLB) |
| `sill_overhang` | Свес подоконника за раму (для трансформации GLB) |

Каждый GLB-модуль окна включает именованные дочерние объекты: `frame_left`,
`frame_right`, `frame_top`, `frame_bottom`, `sill`, `glass`, `curtain`
(и `mullion_v`, `mullion_h` для двустворчатых/панорамных). Код трансформирует их
для достижения целевого размера (см. `HOUSE_MODULES_SPEC.md`, раздел 2.5).

#### Дверь

```jsonc
{ "door": "single" }                       // распашная одностворчатая
{ "door": "onehalf" }                      // распашная полуторастворчатая
{ "door": "double" }                       // распашная двустворчатая
{ "door": "slide_single" }                 // раздвижная одностворчатая
{ "door": "slide_double" }                 // раздвижная двустворчатая
{ "door": "single", "w": 0.80 }            // с переопределением ширины
{ "door": "slide_double", "w": 2.40 }      // широкая раздвижная
{ "door": "single", "main": true }         // главный вход — к ней привязывается крыльцо (features.porch)
```

Двери всегда начинаются от пола (y = 0).

**Флаг `"main": true`** помечает главный вход. Используется builder'ом крыльца (`features.porch`) для выбора двери, перед которой ставить крыльцо. Если ни одна дверь не помечена `main` — крыльцо привязывается к первой двери в обходе периметра 1-го этажа. Если дверей в доме нет — крыльцо не строится (warn).

| Тип | Створки | Механизм | Дефолт w | Дефолт h |
|-----|:-------:|----------|:--------:|:--------:|
| `single` | 1 | распашная | 0.90 м | 2.10 м |
| `onehalf` | 1.5 | распашная | 1.20 м | 2.10 м |
| `double` | 2 | распашная | 1.40 м | 2.10 м |
| `slide_single` | 1 | раздвижная | 1.00 м | 2.10 м |
| `slide_double` | 2 | раздвижная | 2.00 м | 2.30 м |

Каждый GLB-модуль двери включает именованные дочерние объекты: `frame_left`,
`frame_right`, `frame_top`, `threshold`, `leaf_main`, `handle`
(и `leaf_minor` для полуторастворчатых/двустворчатых, `rail_top`, `rail_bottom` для раздвижных).

---

## 7. Мансардные и слуховые окна (`roof_windows`)

Секция `roof_windows` задаёт окна, размещаемые на скатах крыши.
Два типа: **velux** (в плоскости ската) и **dormer** (люкарна, выступает из ската).

```jsonc
"roof_windows": [
  {
    "module": "window_velux",
    "rect_index": 0,             // на каком rect стоит окно (декомпозиция полигона)
    "slope": "south",
    "position_along": 0.5,
    "position_up": 0.5,
    "w": 0.78,
    "h": 0.98,
    "count": 2,
    "spacing": 1.5
  },
  {
    "module": "dormer",
    "rect_index": 0,
    "slope": "south",
    "position_along": 0.3,
    "position_up": 0.3,
    "w": 1.50,
    "h": 1.40,
    "depth": 1.00,
    "window": {
      "type": "single",
      "w": 0.90,
      "h": 1.00
    }
  }
]
```

### 7.1. Общие поля

| Поле | Тип | Обязательное | Описание |
|------|-----|:---:|----------|
| `module` | string | ✓ | ID модуля: `"window_velux"` или `"dormer"` |
| `slope` | string | ✓ | На каком скате: `"north"`, `"south"`, `"east"`, `"west"` |
| `position_along` | number | ✓ | Позиция вдоль конька (0.0 = левый край, 1.0 = правый край) |
| `position_up` | number | ✓ | Позиция вверх по скату (0.0 = карниз, 1.0 = конёк). Для **dormer** это позиция **основания** люкарны (центра нижнего края) на скате. |
| `rect_index` | number | — | Индекс прямоугольника после декомпозиции полигона (0 = главный/самый большой). По умолчанию `0`. Для прямоугольных домов всегда `0`. |
| `count` | number | — | Количество окон в ряд (по умолчанию 1) |
| `spacing` | number | — | Расстояние между центрами окон вдоль конька, метры (нужен при `count > 1`) |

**Поддержка типов крыш в первой версии:**

| roof_type | velux | dormer | Какие slope доступны |
|-----------|:-----:|:------:|----------------------|
| `hip` | ✓ | ✓ | все 4 (`north`/`south`/`east`/`west`) |
| `gable` (главный rect) | ✓ | ✓ | только 2 длинных ската (по длинной оси) |
| `gable_cross` | — | — | TODO |
| `flat` | — | — | нет скатов |

Для `gable`-крыши на торцах (по короткой оси) — фронтоны, не скаты; velux/dormer туда не ставятся. Builder выдаёт warn и пропускает окно при несовместимом `slope`.

**Velux** накладывается на скат поверху: рама поднята на 6 см над плоскостью, custom flat glass на 8.5 см над скатом. CSG-вырез в геометрии ската НЕ нужен — стекло выше плоскости.

**Dormer** строится процедурно: BoxGeometry (стены) + BufferGeometry (двускатная мини-крыша с 2 скатами и 2 фронтонами). Конёк перпендикулярен главному коньку. Утопление в основной скат через `basePt.y -= (d/2) * tan(slope_angle)` — передняя нижняя часть садится на скат, задняя глубже. Условие скрытия задней мини-крыши под скатом: `d ≥ h / tan(angle) + w/2`. GLB-стекло и подоконник окна (`glass`, `sill`) скрыты, вместо них custom flat glass перед стеной. CSG-вырез в скате НЕ нужен — внутрь не заглядываем.

### 7.2. Velux (мансардное окно в плоскости ската)

| Поле | Тип | Описание |
|------|-----|----------|
| `w` | number | Ширина окна, метры |
| `h` | number | Высота окна (вдоль ската), метры |

Значения `w` и `h` должны быть в пределах диапазонов из `modules.window_velux`.

Код при установке:
1. Вычисляет 3D-позицию на скате по `position_along` и `position_up`
2. Вырезает прямоугольник в геометрии ската
3. Клонирует и трансформирует GLB-модуль
4. Размещает в плоскости ската

### 7.3. Dormer (слуховое окно / люкарна)

| Поле | Тип | Описание |
|------|-----|----------|
| `w` | number | Ширина фронта люкарны, метры |
| `h` | number | Высота стенки люкарны, метры |
| `depth` | number | Глубина выступа от ската, метры |
| `window` | object | Параметры встроенного окна (см. ниже) |

**Встроенное окно** (`window`):

```jsonc
"window": {
  "type": "single",    // тип окна (single, double)
  "w": 0.90,           // ширина (должна быть < w люкарны)
  "h": 1.00            // высота (должна быть < h люкарны)
}
```

Код при установке:
1. Вычисляет 3D-позицию основания на скате
2. Вырезает область в геометрии ската
3. Клонирует и трансформирует GLB-модуль dormer (стенки, мини-крыша)
4. Трансформирует встроенное окно внутри dormer
5. Размещает конструкцию, ориентируя фронт наружу

---

## 8. Дополнительные элементы (`features`)

```jsonc
"features": {
  "chimney":             { "position": [0.3, 0.6], "model": "chimney", "drop": 0.5 },
  "gutters":             true,
  "cornice":             true,
  "inter_floor_cornice": true,                                // или { "depth": 0.20 }
  "porch": {
    "width":           2.4,        // м, ширина крыльца (вдоль стены)
    "depth":           1.5,        // м, выступ от стены
    "step_rise":       0.18,       // м, высота одной ступеньки (опц., по умолчанию 0.18)
    "step_run":        0.30,       // м, глубина каждой ступеньки (опц., по умолчанию 0.30)
    "has_canopy":      true,       // плоский навес на колоннах (опц., default true)
    "canopy_height":   2.40,       // м, высота от пола площадки до низа навеса (опц., default 2.4)
    "canopy_slope":    0.12,       // м, перепад высот навеса (стена выше, ступени ниже)
    "has_railing":     true,       // перила по бокам (опц., default true)
    "railing_height":  0.95        // м, высота перил (опц., default 0.95)
  }
}
```

| Ключ | Тип | Описание |
|------|-----|----------|
| `chimney` | object \| false | Дымоход. `position` — нормализованные координаты [0..1] на крыше. `model` — ID GLB-модуля. `drop` (м) — насколько труба «утопает» в крышу (по умолчанию 0.5). |
| `gutters` | boolean | Водосточная система. Рендерится только если `cornice` НЕ включён (визуально дублируется). |
| `cornice` | boolean | Карнизный свес по периметру стен под крышей. **TODO**: требуется GLB `mod_cornice_corner.glb` с трапециевидным сечением, повторяющим профиль `mod_cornice.glb`, для корректного закрытия convex-углов. Без этого углы остаются с зазором `cornice_depth × cornice_depth`. |
| `inter_floor_cornice` | boolean \| object | Межэтажный карниз — выступающая за стены плита толщиной 20 см на стыке этажей. Также служит intermediate roof для 1.5-этажа (покрывает выступающие части первого этажа). Объектный вид: `{ "depth": 0.05 }` задаёт выступ наружу за периметр стен (по умолчанию 0.05 м). Использует материал `mat_cornice` — синхронно с верхним карнизом. |
| `porch` | object | Крыльцо у входной двери. Привязывается **к двери** в фасаде (а не к стороне дома). По умолчанию — к первой двери в обходе периметра 1-го этажа; если в фасаде помечена дверь флагом `"main": true`, то к ней. Builder автоматически вычисляет позицию двери, ширину/глубину площадки, ставит ступени по высоте фундамента (шаг `step_rise`), 2 колонны по передним углам, плоский навес с наклоном в сторону ступеней (`canopy_slope`), и перила по бокам (если `has_railing`). См. также поле `"main": true` в `door`-элементе фасада (раздел 6.3). |

#### Поля крыльца (`features.porch`)

| Поле | Тип | По умолч. | Описание |
|------|-----|-----------|----------|
| `width` | number | **обязательное** | Ширина крыльца вдоль стены, м |
| `depth` | number | **обязательное** | Выступ крыльца от стены наружу, м |
| `offset_along` | number | `0` | Смещение центра крыльца вдоль стены от центра двери, м. Положительное — в направлении обхода периметра (CW), отрицательное — против. Нужно, когда дверь близко к углу дома, чтобы крыльцо не выходило за стену. |
| `step_rise` | number | `0.18` | Целевая высота одной ступени, м. Реальная высота — `(baseH - nosing_thick) / stepCount`, где `stepCount = ceil((baseH - nosing_thick) / step_rise)` |
| `step_run` | number | `0.30` | Глубина каждой ступени, м |
| `has_canopy` | boolean | `true` | Плоский навес на колоннах |
| `canopy_height` | number | `2.40` | Высота от уровня площадки крыльца до низа навеса, м |
| `canopy_slope` | number | `0.12` | Перепад высот навеса (у стены выше, у ступеней ниже), м |
| `has_railing` | boolean | `true` | Перила по двум боковым сторонам площадки (между стеной и передними колоннами) |
| `railing_height` | number | `0.95` | Высота перил, м |

Материалы (имена `mat_*` в `materials_map`): колонны — `mat_porch_column`, ступени — `mat_porch_step`, навес — `mat_porch_canopy`, перила — `mat_porch_railing`. Если этих ключей нет в `materials_map`, builder использует общие fallback-материалы (`mat_wood`, `mat_concrete`, `mat_metal`).

Все элементы `features` опциональны.

### 8.1. Мансарда (`mansard`, при `roof_type: "mansard"`)

Мансарда — это **классическая ломаная крыша Мансар** (не отдельный этаж, а тип крыши). Состоит из двух пар скатов: нижний крутой (60–75°) от карниза до излома и верхний пологий (20–35°) от излома до конька. Жилое пространство мансарды находится под скатами; характерный «дом-силуэт» с переломом на полпути к коньку.

```jsonc
"roof_type": "mansard",
"mansard": {
  "knee_height":  0.20,   // м, доп. вертикальная стенка между потолком этажа и началом нижнего ската
  "lower_angle":  70,     // град, угол нижнего крутого ската
  "upper_angle":  25,     // град, угол верхнего пологого ската
  "lower_height": 2.20    // м, высота нижнего ската (от излома до eave-линии)
}
```

| Поле | Тип | По умолч. | Описание |
|------|-----|-----------|----------|
| `knee_height` | number | `0` | Высота короткой вертикальной стенки между верхом основного этажа и началом нижнего ската, м. Knee wall строится из тех же модулей, что и стены этажа (`mod_wall_segment.glb`, `mod_pillar.glb`). |
| `lower_angle` | number | `70` | Угол наклона нижнего ската, градусы (40–80°). Чем круче — тем больше полезного пространства внутри мансарды. |
| `upper_angle` | number | `25` | Угол наклона верхнего ската, градусы (10–35°). Чем меньше — тем выше конёк. |
| `lower_height` | number | `2.0` | Высота нижнего ската по вертикали (= высота "этажа" мансарды от потолка нижнего до излома), м. |

**Геометрия:** автоматически считается излом и положение конька:
- `horizontal_lower = lower_height / tan(lower_angle)` — горизонтальное смещение от eave к излому.
- `kinkY = baseY + lower_height` — Y-координата излома.
- `ridgeY = kinkY + (W/2 - horizontal_lower) * tan(upper_angle)` — Y конька.

Если `horizontal_lower ≥ W/2` — нижний скат не помещается; builder выдаёт warn и fallback на обычный gable.

**Фронтоны** (на коротких торцах) — пятиугольники (5 вершин: 2 базы + 2 излома + 1 ridge).

**Окна мансарды:**
- На нижних крутых скатах — `roof_windows` с `module: "window_velux"` или `module: "dormer"` (см. раздел 7). Frame ската описывает **нижний** скат от eave до kink. На крутом скате (70°) рекомендуется небольшая глубина dormer'а (≤1.5 м), иначе он не помещается.
- На верхних пологих скатах — TODO, пока не поддерживается.
- На вертикальных фронтонах — TODO, пока не реализовано.

**Декомпозиция полигона:** для многосоставной формы (L, П и т.п.) главный (самый большой) rect получает ломаную мансардную крышу, остальные — вальмовые (hip) скаты с углом `lower_angle`.

### Координаты дымохода

`"position": [0.3, 0.6]` означает:
- 30% длины дома от левого края
- 60% ширины дома от верхнего края (на плане)

Код вычисляет абсолютные координаты и размещает GLB-модуль на поверхности крыши.

---

## 9. Карта материалов (`materials_map`)

```jsonc
"materials_map": {
  "mat_wall":     { "label": "Стены",              "swappable": true },
  "mat_base":     { "label": "Цоколь",             "swappable": true },
  "mat_roof":     { "label": "Крыша",              "swappable": true },
  "mat_frame":    { "label": "Рамы окон",          "swappable": true },
  "mat_door":     { "label": "Дверь",              "swappable": true },
  "mat_wood":     { "label": "Дерев. отделка",     "swappable": true },
  "mat_metal":    { "label": "Металл (водосток)",  "swappable": true },
  "mat_concrete": { "label": "Бетон (ступени)",    "swappable": true },
  "mat_curtain":  { "label": "Шторы",              "swappable": true },
  "mat_flashing": { "label": "Оклад (мансардное)", "swappable": true }
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| ключ | string | Имя материала в GLB (обязательно начинается с `mat_`) |
| `label` | string | Отображаемое название в UI |
| `swappable` | boolean | Можно ли пользователю менять этот материал |

Код при загрузке GLB-модулей сканирует все материалы с именами `mat_*` и сопоставляет
с этой картой. Материалы с `"swappable": true` показываются в палитре выбора.

Новые типы домов могут вводить дополнительные материалы — достаточно добавить
запись в `materials_map` и использовать соответствующее имя в Blender.

---

## 10. Примеры планировок

### 10.1. Прямоугольный дом (4 run, 4 turn)

```
┌────────────────┐
│                │
│                │
│                │
└────────────────┘
```

```jsonc
"perimeter": [
  { "run": "L", "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },
  { "run": "W", "facade": [
      { "wall": "fill" }, { "door": "single" }, { "wall": "fill" }
  ]},
  { "turn": 90 },
  { "run": "L", "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },
  { "run": "W", "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 }
]
```

**Переменные**: `"L": "sqrt(area * 1.6)"`, `"W": "sqrt(area / 1.6)"`

### 10.2. Г-образный (L-shaped) дом (6 run, 6 turn)

```
┌──────────┐
│          │
│          ├────┐
│          │    │
└──────────┴────┘
```

```jsonc
"vars": {
  "L":   "sqrt(area * 1.4)",
  "W":   "sqrt(area / 1.4)",
  "ext": "L * 0.35",
  "ew":  "W * 0.4"
}
```

```jsonc
"perimeter": [
  { "run": "L",   "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },
  { "run": "W - ew", "facade": [{ "wall": "fill" }] },
  { "_comment": "выступ наружу" },
  { "turn": -90 },
  { "run": "ext", "facade": [
      { "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }
  ]},
  { "turn": 90 },
  { "run": "ew",  "facade": [
      { "wall": "fill" }, { "door": "single" }, { "wall": "fill" }
  ]},
  { "turn": 90 },
  { "run": "L + ext", "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },
  { "run": "W",  "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 }
]
```

### 10.3. Крестообразный дом (12 run, 12 turn)

```
       ┌─────┐
       │  N  │
  ┌────┤     ├────┐
  │ W  │     │ E  │
  └────┤     ├────┘
       │  S  │
       └─────┘
```

```jsonc
"vars": {
  "core": "sqrt(area * 0.6)",
  "arm":  "sqrt(area * 0.1)"
}
```

```jsonc
"perimeter": [
  { "_comment": "── N верх →" },
  { "run": "core", "facade": [
      { "wall": 0.8 }, { "window": "single" }, { "wall": "fill" },
      { "window": "single" }, { "wall": 0.8 }
  ]},
  { "turn": 90 },

  { "_comment": "── N→E спуск ↓" },
  { "run": "arm", "facade": [{ "wall": "fill" }] },
  { "turn": -90 },

  { "_comment": "── E верх →" },
  { "run": "arm", "facade": [
      { "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }
  ]},
  { "turn": 90 },

  { "_comment": "── E правый бок ↓" },
  { "run": "core", "facade": [
      { "wall": 0.8 }, { "window": "single" }, { "wall": 0.8 }
  ]},
  { "turn": 90 },

  { "_comment": "── E низ ←" },
  { "run": "arm", "facade": [
      { "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }
  ]},
  { "turn": -90 },

  { "_comment": "── E→S спуск ↓" },
  { "run": "arm", "facade": [{ "wall": "fill" }] },
  { "turn": 90 },

  { "_comment": "── S низ ←" },
  { "run": "core", "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },

  { "_comment": "── S→W подъём ↑" },
  { "run": "arm", "facade": [{ "wall": "fill" }] },
  { "turn": -90 },

  { "_comment": "── W низ ←" },
  { "run": "arm", "facade": [
      { "wall": 0.6 }, { "door": "single" }, { "wall": 0.6 }
  ]},
  { "turn": 90 },

  { "_comment": "── W левый бок ↑" },
  { "run": "core", "facade": [
      { "wall": 0.8 }, { "window": "single" }, { "wall": 0.8 }
  ]},
  { "turn": 90 },

  { "_comment": "── W верх →" },
  { "run": "arm", "facade": [
      { "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }
  ]},
  { "turn": -90 },

  { "_comment": "── W→N подъём ↑ (замыкание)" },
  { "run": "arm", "facade": [{ "wall": "fill" }] },
  { "turn": 90 }
]
```

### 10.4. П-образный дом (8 run, 8 turn)

```
┌────┐       ┌────┐
│    │       │    │
│    └───────┘    │
│                 │
└─────────────────┘
```

```jsonc
"vars": {
  "L":     "sqrt(area * 1.3)",
  "W":     "sqrt(area / 1.3)",
  "wing":  "L * 0.25",
  "notch": "W * 0.4"
}
```

```jsonc
"perimeter": [
  { "run": "wing",         "facade": [{ "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }] },
  { "turn": 90 },
  { "run": "notch",        "facade": [{ "wall": "fill" }] },
  { "turn": -90 },
  { "run": "L - wing * 2", "facade": "auto_windows", "window_type": "single" },
  { "turn": -90 },
  { "run": "notch",        "facade": [{ "wall": "fill" }] },
  { "turn": 90 },
  { "run": "wing",         "facade": [{ "wall": 0.6 }, { "window": "single" }, { "wall": 0.6 }] },
  { "turn": 90 },
  { "run": "W",            "facade": [
      { "wall": "fill" }, { "door": "single" }, { "wall": "fill" }
  ]},
  { "turn": 90 },
  { "run": "L",            "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 },
  { "run": "W",            "facade": "auto_windows", "window_type": "single" },
  { "turn": 90 }
]
```

### 10.5. Пример с мансардными окнами

```jsonc
{
  "id": "type_example_attic",
  "name": "Дом с мансардой",
  "roof_type": "gable",
  "modules": { /* ... */ },
  "floors": [
    {
      "id": "floor_1",
      "label": "Первый этаж",
      "constraints": { "area": { "min": 50, "max": 120, "default": 80 }, "floor_h": { "min": 270, "max": 320, "default": 300, "unit": "cm" } },
      "vars": { "L": "sqrt(area * 1.6)", "W": "sqrt(area / 1.6)" },
      "perimeter": [
        { "run": "L", "facade": "auto_windows", "window_type": "double" },
        { "turn": 90 },
        { "run": "W", "facade": [{ "wall": "fill" }, { "door": "double" }, { "wall": "fill" }] },
        { "turn": 90 },
        { "run": "L", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "W", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 }
      ]
    }
  ],
  "roof_windows": [
    {
      "module": "dormer",
      "w": 1.50,
      "h": 1.40,
      "depth": 1.00,
      "slope": "south",
      "position_along": 0.5,
      "position_up": 0.35,
      "count": 2,
      "spacing": 3.0,
      "window": { "type": "single", "w": 0.90, "h": 1.00 }
    },
    {
      "module": "window_velux",
      "w": 0.78,
      "h": 0.98,
      "slope": "north",
      "position_along": 0.5,
      "position_up": 0.5,
      "count": 3,
      "spacing": 1.5
    }
  ]
}
```

### 10.6. Пример с разными окнами по этажам

```jsonc
{
  "id": "type_e",
  "name": "Двухэтажный дом",
  "modules": {
    "window_single": {
      "w": { "min": 0.60, "max": 1.40, "default": 0.90 },
      "h": { "min": 0.60, "max": 1.80, "default": 1.20 },
      "y": { "min": 0.30, "max": 1.50, "default": 0.90 },
      "frame_profile": 0.05,
      "sill_overhang": 0.03,
      "glb": "mod_window_single.glb"
    }
  },
  "floors": [
    {
      "id": "floor_1",
      "label": "Первый этаж",
      "modules_override": {
        "window_single": { "h": 1.50, "y": 0.70 }
      },
      "constraints": { "area": { "min": 50, "max": 120, "default": 90 }, "floor_h": { "min": 280, "max": 320, "default": 300, "unit": "cm" } },
      "vars": { "L": "sqrt(area * 1.6)", "W": "sqrt(area / 1.6)" },
      "perimeter": [
        { "run": "L", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "W", "facade": [{ "wall": "fill" }, { "door": "double" }, { "wall": "fill" }] },
        { "turn": 90 },
        { "run": "L", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "W", "facade": [
            { "wall": 1.0 },
            { "window": "single", "w": 0.60, "h": 0.50, "y": 1.50 },
            { "wall": "fill" },
            { "window": "single" },
            { "wall": 1.0 }
        ]},
        { "turn": 90 }
      ]
    },
    {
      "id": "floor_2",
      "label": "Второй этаж",
      "modules_override": {
        "window_single": { "h": 1.20, "y": 0.90 }
      },
      "constraints": { "area": { "min": 40, "max": 100, "default": 70 }, "floor_h": { "min": 250, "max": 300, "default": 280, "unit": "cm" } },
      "vars": { "L2": "sqrt(area * 1.5)", "W2": "sqrt(area / 1.5)" },
      "perimeter": [
        { "run": "L2", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "W2", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "L2", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 },
        { "run": "W2", "facade": "auto_windows", "window_type": "single" },
        { "turn": 90 }
      ]
    }
  ]
}
```

В этом примере:
- **1-й этаж**: окна выше (h=1.50) с низким подоконником (y=0.70), плюс маленькое окно санузла
- **2-й этаж**: стандартные окна (h=1.20, y=0.90)
- Приоритет: маленькое окно санузла `{ "w": 0.60, "h": 0.50, "y": 1.50 }` переопределяет и `modules_override`, и корневой `modules`

---

## 11. Валидация дескриптора

Код при загрузке JSON проверяет:

| Проверка | Ошибка |
|----------|--------|
| Контур замкнут | `"Контур не замкнут: Δx=..., Δz=..."` |
| Все переменные в `run` определены в `vars` | `"Неизвестная переменная: ..."` |
| `fill` не даёт отрицательных значений | `"Элементы не помещаются в run длиной ..."` |
| Если в фасаде нет `fill`, `Σ(fixed) ≈ wallLength` | `"⚠ no fills, but gap N м (wall=…, fixed=…). Добавь {wall:fill}"` (warn, не error) |
| Все модули (`window`, `door`) есть в `modules` | `"Модуль не найден: ..."` |
| Переопределённые размеры в диапазоне `min..max` | `"Размер вне диапазона: window_single.w = 2.0, max = 1.40"` |
| `constraints` содержит `min`, `max`, `step`, `default` | `"Неполное ограничение: ..."` |
| `min < default < max` | `"default вне диапазона: ..."` |
| `step > 0` | `"Некорректный шаг: ..."` |
| `materials_map` ключи начинаются с `mat_` | `"Материал без префикса mat_: ..."` |
| Все повороты кратны 90° | `"Поворот не кратен 90°: ..."` (пока поддерживаем только 90°) |
| `floors` не пуст | `"Нет этажей"` |
| `roof_windows` — `w`, `h` в допустимых диапазонах | `"Размер мансардного окна вне диапазона: ..."` |
| `roof_windows` — `position_along` и `position_up` в [0, 1] | `"Позиция окна на крыше вне диапазона: ..."` |
| `dormer.window.w < dormer.w` | `"Окно люкарны шире самой люкарны: ..."` |
| `dormer.window.h < dormer.h` | `"Окно люкарны выше стенки люкарны: ..."` |

---

## 12. Шпаргалка: как создать дескриптор нового дома

1. **Нарисуйте план на бумаге** (вид сверху)

2. **Определите переменные** — какие размеры зависят от площади:
   - Для прямоугольника: `L = sqrt(area * ratio)`, `W = sqrt(area / ratio)`
   - Для сложного плана: выделите основные размеры (core, wing, arm...)

3. **Обойдите периметр** по часовой стрелке от левого верхнего угла:
   - Каждый прямолинейный участок = `run`
   - Каждый поворот = `turn` (+90 внутрь, -90 наружу)
   - Помните: каждый `turn` = столб, длина стены = run - pillar_size

4. **Настройте модули** — задайте диапазоны размеров для окон и дверей:
   - Разные размеры на разных этажах → `modules_override`
   - Уникальные окна (санузел, кладовая) → переопределение в элементе фасада

5. **Опишите фасад** каждого run:
   - Расставьте окна и двери
   - Заполните промежутки: `{ "wall": 1.2 }` или `{ "wall": "fill" }`
   - Или используйте `"auto_windows"` для автоматической расстановки

6. **Добавьте мансардные окна** (если крыша не плоская):
   - Velux — для функциональных окон (свет и вентиляция)
   - Dormer — для декоративных / жилых мансардных окон

7. **Проверьте замкнутость** — мысленно пройдите весь контур и убедитесь,
   что вернулись в начальную точку

8. **Задайте ограничения** — допустимые диапазоны площади и высоты

9. **Проверьте масштабирование** — мысленно подставьте min и max площади
   и убедитесь, что все `fill` остаются положительными

---

## 13. Связь с другими файлами проекта

| Файл | Использует дескриптор для... |
|------|-----------------------------|
| `viewer3d-core.js` | `buildHouseFromDescriptor()` — 3D-сборка из модулей |
| `state.js` | `S.houseType` = ID дескриптора, `S.wallZones` = назначения материалов |
| `nav.js` | Шаги 1–3 UI: constraints из дескриптора → ползунки/поля |
| `ui.js` | Шаг «Отделка фасада»: `materials_map` → палитра, `segId` → подсветка |
| `HOUSE_MODULES_SPEC.md` | Общая архитектура модульной системы, соглашения по GLB |

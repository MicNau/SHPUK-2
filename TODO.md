# TODO

Ревизия 2026-08-22. Список от 2026-08-21 пройден полностью; ниже — что закрыто и
что осталось открытым. Нумерация сохранена, чтобы сверяться с исходным списком.

## Открыто

### На стороне бэкенда (фронт не обходит — по договорённости)

- **Баг 3.** Нет тамбнэйлов и превью мебели: `preview_picture` / `detail_picture`
  приходят с `url: null`.
- **Баг 4.** Не работает превью у грядок, отделки фасада, частично у ограждений
  террасы и дорожек: `texture_urls` бэкенд отдаёт ТОЛЬКО у товаров с тегом, а
  разделы 2330 (ступени) и 2357 (грядки) товаров пока не отдают вовсе.
- **Баг 5.** То же у заборов.

Разбор с примерами ответов — `ARCHITECTURE.md`, раздел «Превью и фото товаров —
чего не хватает на бэкенде». Как только картинки и теги появятся, фронт подхватит
их без правок.

### Проверка расчёта на живом бэкенде

Весь расчёт (терраса, отдельно стоящие террасы, ступени, дорожки, забор,
ограждение, мебель) собран по контракту и проверен на структуре запроса, но из
песочницы разработки `sollersdev.ru` недоступен — живого ответа никто не видел.
Нужен прогон на стенде с доступом.

### Своих типов расчёта в API нет

Фасад и грядки считает клиентский `_computeEstimate` — в `calculate_project/`
таких объектов нет.

## Закрыто

### Баги

| № | Что было | Как закрыто |
|---|---|---|
| 1 | Полигонфайтинг на коньке полутораэтажного дома | Убран совпадающий торцевой квад в `buildSlopeBox` |
| 2 | В каталоге нельзя выбрать несколько ценовых опций | `S.catPrices` — множество, фильтр по объединению |
| 6 | В фильтре цвета цвета без товаров | Показываем только цвета, у которых есть позиции (`_availableColors`) |

Баги 3–5 — см. «Открыто».

### UX/UI

| № | Пункт | Статус |
|---|---|---|
| 1 | Чёрный курсор на canvas | SVG-стрелка вместо системного курсора |
| 2 | Тексты подсказок редакторов | Переписаны, у бассейна и причала — по образцу террасы |
| 3 | Убрать «Загрузить фото дома» | Убрано вместе с логикой |
| 4 | Ступени зашиты всегда | Обшивка каркаса безусловна |
| 5 | Материал перил = материал ограждения террасы | Перила берут материал ограждения один в один (одна база, одни карты) |
| 6 | Лупа → кнопка «Посмотреть» | Кнопка слева от «Применить», фото открывается модалкой с затемнением |
| 7 | «Применить» гаснет после применения | Неактивное состояние по соглашению меню |
| 8 | Фундамент и стены — только цвет | Фундамент: бежевый / коричневый / тёмно-серый; стены: белый / бежевый / коричневый |
| 9 | Боковины террасы: поворот текстуры на 90°, UV-бокс 1.3 м | `TERRACE_SIDE_TILE = 1.3` |
| 10 | Залипание ступеней к столбам ограждения | Снап учитывает смещение перил от кромки; работает при перемещении, resize и открытии редактора |
| 11 | Убрать «Добавить» в редакторе мебели | Убрано |
| 12 | «Терраса у бассейна» и «Причал» — редактор террасы | Один прямоугольный редактор на три секции (`RECT_SECTIONS`); обе — отдельно стоящие |
| 13 | Убрать «Ограждение бассейна» | Опции нет |
| 14 | Активные элементы оранжевым во всех редакторах | Акцентный цвет у редактируемого элемента |
| 15 | Без скруглений у панелей и окон | Скругления сняты |
| 16 | Косметика левой панели редакторов | Пробелы, «Навес», слайдер ширины дорожки, статистика, подписи, разделитель, позиция заголовков |

### Сверх списка

- Дорожки уходят в расчёт проекта: осевая + ширина → замкнутый контур ленты с
  опорной стороной (раньше в смету бэкенда не попадали вовсе).
- Терраса у бассейна и причал считаются на бэкенде типом `terrace` со всеми
  вершинами `free`.
- Зашивка (щёки) и подступенки лестницы — из материала террасы; у щёк появились
  UV, без которых текстура товара на них не ложилась.
- Каталог у трёх террас общий (раздел 2314 + палитра террасной доски), а доска и
  строка сметы — у каждой своя.

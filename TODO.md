1. ВСЕ надписи везде должны делаться ТОЛЬКО шрифтом Segoe UI, меняется только кегль и начертание
2. На первом экране кнопку "Дальше" делаем стандартного размера и ставим справа от панели "Участок без дома"
3. После "Удалить всё" раздел должен обнуляться (все объекты удаляются, раздел закрывается, кнопка "Изменить" убирается)
4. Во всех разделах, где есть слайдеры, ставим их ниже кнопок, что бы задействовать всю ширину
5. Когда раздел открыт, кнопки ставим ниже кнопки раздела, что бы не укорачивать её
6. Материал ограждения террасы делаем как у самой террасы
7. Дорожки: при совпадении точек замыкаем контур. При пересечении дорожек объединяем  полигоны
8. Когда никакой раздел редактора не раскрыт кнопку "Назад" называем "К параметрам дома"
9. Когда раздел раскрыт кнопку "Назад" не показываем
10. В каталоге центрировать открытые карточки не нужно. Если карточка самая нижняя -- при раскрытии она должна всплывать так, что бы были видны кнопки
11. "Запретную зону" для забора заливать не нужно, достаточно общего контура
12. "Высота забора" стоит написать в одну строчку, а обе кнопки с высотами поместить под ней. Они должны быть одинакового размера
13. Калитка сейчас не управляется. И должна быть возможность удалять её как обычные точки
14. Сегменты забора сейчас не стыкуются:![[Pasted image 20260825164758.png|477]]
15. Грядки: фильтры должны позволять мультивыбор
16. Бассейн: в 3D лучше показывать просто цилиндром или боксом (сейчас "поверхность воды" съезжает). Полуступень нужно подрезать так же как всю террасу:![[Pasted image 20260825170052.png|620]]

---

### Статус (ведёт фронт)

Сделаны все пункты 1–16.

П.7: контур замыкается кликом по первой точке; перекрытия дорожек вычитаются —
осевая режется там, где её накрывает полотно другой дорожки, и по этим же линиям
считаются и площадь в панели, и полигоны в задании на расчёт. Крест 12 м + 8 м
при ширине 1.2 м: было 24.0 м², стало 22.56 м² (вычлось пятно 1.2 × 1.2 м).

Попутно: `dToast` вызывался из редакторов, но нигде не был определён — часть сообщений
не показывалась, а «Поставить бассейн», «Калитка» и «Обозначить вход» падали с ошибкой.
Функция добавлена.

По п.6: наследование материала террасы ограждением ОТМЕНЕНО следующей правкой
(см. ниже, п.3) — до выбора своего товара ограждение серое.
По п.15: фильтры только отбирают каталог, на 3D не влияют — до выбора товара грядка
обозначается местом, после рисуется по товару (высота борта берётся из него).
Попутно починены регулярки крепежа: `\w` в JS не покрывает кириллицу, поэтому
«пластиковый угол» и «металлический угол» не совпадали никогда.
По п.14: если в GLB столб слит с горизонтальными прогонами в один меш, распознать его
нечем — на свободном конце встанет простой box-столб. Это видно в консоли по строке
`[fence] столбы в модели: не распознаны`; лечится разделением мешей в модели.

---

### Правки после этапа 2 (сделано)

1. Фильтры ограждения (крышка, сечение столба) — фильтры каталога с мультивыбором.
   Сечение столба в 3D берётся из выбранного товара, а не из фильтра.
2. Терраса встаёт вплотную к стене (кромка у дома не снапится на сетку 0.5 м).
3. Ограждение до выбора своего товара — условный серый (наследование материала
   террасы отменено).
4. Терраса у бассейна до выбора досок — тоже условный серый.
5. «Ограждения террасы» — в одну строку и в меню, и в заголовке правой панели;
   ширина обеих панелей считается по реальным метрикам шрифта.
6. У 3D-окна убраны скругления углов.
